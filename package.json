{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "apps/ic-dental",
    "packages/api-client-react"
  ],
  "scripts": {
    "dev": "npm run dev -w @workspace/ic-dental",
    "build": "npm run build -w @workspace/ic-dental",
    "serve": "npm run serve -w @workspace/ic-dental",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck -w @workspace/ic-dental"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
